app = "kirogate"
primary_region = "nrt"

[build]
  # 使用外部已构建镜像时可以为空
  # image = "registry.fly.io/kirogate:latest"

[deploy]
  strategy = "immediate"

[env]
  USER_DB_FILE = "/app/data/users.db"
  # 推荐：国外服务器关闭静态资源代理，直连 CDN 更快
  # STATIC_ASSETS_PROXY_ENABLED = "false"

[http_service]
  internal_port = 8000
  force_https = true

  auto_start_machines = true
  auto_stop_machines = "stop"

  # 🔴 调试阶段强烈建议 �?1
  min_machines_running = 1

  processes = ["app"]

  # HTTP 超时配置（支持长时间 AI 推理，单位：秒）
  [http_service.http_options]
    # 响应超时�?0 分钟（适配 opus-4-5 等慢模型�?
    response.header.timeout = 600
    # 空闲读取超时�?0 分钟（流式输出）
    read_timeout = 600
    # 空闲写入超时�? 分钟
    write_timeout = 60
    # 连接空闲超时�?0 分钟
    idle_timeout = 600

[[mounts]]
  source = "kirogate_data"
  destination = "/app/data"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256
